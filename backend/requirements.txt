fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]>=3.2.10
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
sse-starlette==2.1.3

# --- Real integrations (§9.2 seams) ---
# httpx: hand-written HTTP agent client + Langfuse public REST API.
httpx==0.28.1
# openai: any OpenAI-compatible endpoint (self-hosted included) via base_url.
openai==1.59.6
# PyJWT: verifies Keycloak access tokens (AUTH_MODE=keycloak). The [crypto] extra
# pulls in `cryptography` for RS256. Its bundled PyJWKClient is deliberately NOT
# used — that one fetches the key set over blocking urllib, which would stall the
# event loop on every cache miss; app/keycloak.py fetches with httpx instead.
PyJWT[crypto]==2.10.1

# --- Dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.2
respx==0.22.0
