[pytest]
testpaths = tests
# The app package lives next to this file; pytest does not add the rootdir to
# sys.path on its own.
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
