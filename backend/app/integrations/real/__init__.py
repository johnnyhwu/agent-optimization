"""Real implementations of the four §6.15 seams.

Imported lazily by `app.integrations` so a fake-only deployment never needs an
agent HTTP endpoint, an LLM endpoint or Langfuse to be configured.
"""
