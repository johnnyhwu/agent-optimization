"""Vendored SkillOpt. Upstream code — change it only to keep it working here.

Every module in this package came from https://github.com/microsoft/SkillOpt and
is meant to stay diffable against it. VENDORED.md (one level up) records the
pinned commit and every deliberate difference, file by file.

If you find yourself wanting to improve the algorithm, do it *outside* this
package and call in. The value of vendoring is that "did we change the
algorithm?" has a mechanical answer.
"""
